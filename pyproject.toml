[project]
name = "genai-master-presentation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "jupyterlab (>=4.6.3,<5.0.0)",
    "langchain (==1.4.0)",
    "langchain-openai (==1.6.2)",
    "langgraph (==1.2.11)",
    "openai (==3.13.0)",
    "python-dotenv (==1.2.3)",
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0"]
build-backend = "poetry.core.masonry.api"
